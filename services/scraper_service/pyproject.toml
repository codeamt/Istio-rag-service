[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "scraper-service"
version = "0.1.0"
dependencies = [
    "fastapi",
    "httpx",
    "pydantic"
]
